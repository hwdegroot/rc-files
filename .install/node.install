#!/bin/bash

echo "Installing node/npm"
curl -sL https://deb.nodesource.com/setup_7.x | su -c "bash -"
su -c "apt-get -y install nodejs"
su -c "apt-get -y install build-essential"

echo "Install git tooling"
su -c "npm install -g diff-so-fancy"

